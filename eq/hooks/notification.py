def notify_change(earthquake=None):
    """Notifies the users through some medium that an earthquake has happened.
    earthquake is passed as a parameter in case it is ever possible to dispatch
    notifications ina more geographically efficient way."""

    print("AAAAA!")
    pass
